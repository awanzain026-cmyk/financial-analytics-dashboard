[project]
name = "expense-tracker-api"
version = "0.1.0"
description = "Ledgerly backend — FastAPI + SQLAlchemy + LLM categorization"
requires-python = ">=3.12"

[tool.fastapi]
entrypoint = "main:app"
